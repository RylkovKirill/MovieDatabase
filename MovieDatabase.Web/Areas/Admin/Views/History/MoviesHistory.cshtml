@using MovieDatabase.Web.Areas.Admin.Views.History
@model IEnumerable<MovieHistory>

@{
    Layout = "_HistoryLayout.cshtml";
    ViewData["Title"] = "Movies History";
    ViewData["ActivePage"] = HistoryNavPages.MoviesHistory;
}

<h1>@ViewData["Title"]</h1>

<table class="table table-bordered shadow">
    <thead>
        <tr>
            <th>Movie</th>
            <th>Action</th>
            <th>Date</th>
            <th>Detais</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Action</td>
                <td>@item.Date</td>
                <td>
                    <a asp-action="MovieHistoryDetails" asp-route-id="@item.Id" class="btn btn-primary icon-wrapper">
                        <i class="fas fa-info"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>